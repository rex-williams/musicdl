#include <stdio.h>
#include <curl/curl.h>

int main (int argc, char **argv) {
    if(argc < 2)
    {
        printf("usage: program [search]\n");
        return 1;
    }

    char* query = argv[1];

    printf("query: %s\n", query);

    return 0;
}
